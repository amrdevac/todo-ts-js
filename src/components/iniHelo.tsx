const IniHelo = ({ title, bambang }: { title?: string; bambang?: number }) => {
  return (
    <div>
      <div>{title} </div> ini helo <div>{bambang}</div>
    </div>
  );
};

export default IniHelo;
    